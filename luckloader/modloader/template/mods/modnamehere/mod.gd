extends Reference

const display_name := "<modnamehere>" # Displayed name of your mod (currently only printed to debug log)
const version := "v0.1.0" # Mod version (currently only printed to debug log for info)

func load(modloader: Reference, tree: SceneTree):
	# Code in here will run when the mod is loaded
	# Modloader is a reference to the modloader object (see <TODO: docs URL> for overview of available functionality)
	# Tree is a reference to the active scene tree (see Godot docs for more info)
	pass
